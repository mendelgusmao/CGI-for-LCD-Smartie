import sysdef main(self):    if (sys.argv > 1):        params = argv[1].split("#")        function = params[0]                if (function):            func = gettattr(self, function, None)            if (callable(func)):                print func(**({} if len(params) == 1 else params[1].split(";")))            else:                print "[CGI4LCD] Function '%s' not found." % function        sys.exit()